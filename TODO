parameterize database for query?

before/after hooks for migration

let user define json map instead of metadoc_type in config. metadoc_type fails if user uses a field not named 'type' to filter docs.

logs and error messages

parameterize Caster.config usages to avoid using it like a global
